upstream php-fpm {
    server app:9000;
}

server {
    listen 80 default_server;
    server_name _;

    root /var/www/app;
    index index.php;

    client_max_body_size 64M;

    # Compress text responses on the fly. The HTML and the theme's
    # build/style-index.css are the two largest payloads on every page and
    # both are highly compressible; gzip cuts them ~75-85% over the wire.
    # text/html is always compressed when gzip is on, so it is not listed.
    gzip on;
    gzip_vary on;
    gzip_comp_level 6;
    gzip_min_length 256;
    gzip_proxied any;
    gzip_types
        text/css
        text/javascript
        text/xml
        application/javascript
        application/json
        application/xml
        application/rss+xml
        application/atom+xml
        image/svg+xml;

    # Healthcheck — used by Docker + Dokploy
    location = /healthz {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }

    # WordPress routing
    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    # PHP handler
    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php-fpm;
        fastcgi_index index.php;
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
        fastcgi_read_timeout 300;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
        fastcgi_param HTTP_PROXY "";
    }

    # Static asset caching
    location ~* \.(?:css|js|jpg|jpeg|gif|png|ico|woff2?|svg|webp|avif)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
        try_files $uri =404;
    }

    # Deny access to sensitive files
    location ~ /\.(?!well-known) { deny all; }
    location ~* /(?:wp-config\.php|readme\.html|license\.txt)$ { deny all; }
    location ~* /wp-content/uploads/.*\.php$ { deny all; }

    # Security headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
}
